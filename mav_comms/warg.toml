name = "mav_comms"
description = "DEPRECATED - Replaced by MAVROS - Thin MAVLink wrapper for communicating with ArduPilot/PX4."

depends_on = ["utils"]

[commands]
setup = "uv sync"
test = "uv run --extra dev pytest"
lint = "uv run --extra dev ruff check ."

[ci]
pr = ["lint"]
