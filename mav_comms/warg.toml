name = "mav_comms"
description = "Thin MAVLink wrapper for communicating with ArduPilot/PX4."

depends_on = ["utils"]

[commands]
setup = "uv sync --no-editable"
test = "uv run --extra dev --no-editable pytest"
lint = "uv run --extra dev --no-editable ruff check ."

[ci]
pr = ["lint"]
