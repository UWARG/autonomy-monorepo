name = "airside_comms"
description = "WebSocket bridge between the airside system and the IMS ground station."

depends_on = ["utils"]

[commands]
setup = "uv sync"
lint = "uv run --extra dev ruff check ."

[ci]
pr = ["lint"]
