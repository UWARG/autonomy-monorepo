name = "utils"
description = "Shared constants, enums, and dataclasses used across all Airside modules."

depends_on = []

[commands]
setup = "uv sync --no-editable"
test = "uv run --no-editable --extra dev pytest"
lint = "uv run --no-editable --extra dev ruff check ."

[ci]
pr = ["lint", "test"]
