name = "utils"
description = "Shared constants, enums, and dataclasses used across all Airside modules."

depends_on = []

[commands]
setup = "uv sync"
lint = "uv run --extra dev ruff check ."

[ci]
pr = ["lint"]
