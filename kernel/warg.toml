name = "kernel"
description = "WARG autonomy airside kernel - scheduler and state management"

depends_on = []

[commands]
setup = "uv sync"
build = "uv build"
test = "uv run --extra dev pytest"
lint = "uv run --extra dev ruff check ."

[ci]
pr = ["lint", "test"]
main = ["test", "build"]
